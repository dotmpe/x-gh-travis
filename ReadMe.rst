Test-running travis and GH releases.

Would want to create tag from travis, but would need
external service or multiple jobs somehow to figure if all different envs did
build OK before commencing tag.

